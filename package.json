{
  "name": "assessment-survey-js",
  "version": "0.0.1",
  "description": "typescript implementation of the assessment / survey",
  "main": "bundle.js",
  "scripts": {
    "build": "tsc && tsc-bundle tsconfig.json --exportAs Bundle --entryPoint App ",
		"run": "tsc && tsc-bundle tsconfig.json --exportAs Bundle --entryPoint App && live-server"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/curiouslearning/assessment-survey-js.git"
  },
  "author": "george and willow",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/curiouslearning/assessment-survey-js/issues"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "typescript": "~4.8.3",
    "typescript-bundle": "^1.0.18"
  }
}
