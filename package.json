{
  "name": "assessment-survey-js",
  "version": "0.0.1",
  "description": "typescript implementation of the assessment / survey",
  "main": "bundle.js",
  "scripts": {
    "build": "tsc && tsc-bundle tsconfig.json --exportAs Bundle --entryPoint App --outFile bundle.js",
    "run": "tsc && tsc-bundle tsconfig.json --exportAs Bundle --entryPoint App --outFile bundle.js && live-server"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/curiouslearning/assessment-survey-js.git"
  },
  "author": "curious learning",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/curiouslearning/assessment-survey-js/issues"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "typescript": "~4.8.3",
    "typescript-bundle": "^1.0.18"
  },
  "dependencies": {
    "firebase": "^9.12.1"
  }
}
